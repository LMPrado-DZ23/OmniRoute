- **fix(docs):** every install and deploy command now points at a channel that is actually published. The docs steered users to `:next` — a mutable pre-release tag the Docker guide itself calls "not supported for production" — in 377 runnable examples across the English docs and all 41 locales, including the Quick Start, the first-10-minutes guide, the VM deployment guide and the Caddy HTTPS recipe. They use `:latest` now; `:next` survives only where the text documents the pre-release channel on purpose. The multi-instance compose example referenced `ghcr.io/lmprado-dz23/omniroute:3.8.49`, a tag that was never published — verified with `docker manifest inspect` and corrected to `:3.8.54` ([#63](https://github.com/LMPrado-DZ23/OmniRoute/pull/63))
- **fix(docs):** the README and Quick Start told users the desktop installer was "not published for this fork yet" and the Docker image might not exist. Release v3.8.54 has been public since 2026-09-19 with 15 assets — `.exe`, `.dmg`, `.AppImage` and `.deb`, x64 and arm64 — and `:latest` pulls. Corrected in English and pt-BR ([#63](https://github.com/LMPrado-DZ23/OmniRoute/pull/63))
