- **fix(cli):** every command now calls a route the server actually implements — **39 broken commands → 0**. 23 had the wrong path or verb (`cache clear` posted where the route is `DELETE /api/cache`; the `keys`, `memory`, `nodes`, `oauth`, `quota`, `skills`, `eval`, `tunnel` and `usage budget` families were repointed), 4 were redirected to the real feature, and 12 were removed because the capability does not exist server-side rather than left as a command that always fails. The stale-entry check on the broken list stays, so it can only remain at zero ([#54](https://github.com/LMPrado-DZ23/OmniRoute/pull/54))
- **fix(cli):** `mcpClient` could not talk to the MCP server at all. Streamable HTTP answers 406 unless the POST accepts both `application/json` and `text/event-stream`, and every request after `initialize` must carry the returned `Mcp-Session-Id`; neither was sent, so every MCP-backed command (`combo suggest`, `compression`, `oneproxy`, `resilience`, `skills`) failed against a real server. Also fixed along the way: `combo suggest` sent a body the tool's schema rejects, `memory add` never worked, and paged listings asked for 500 rows against a 200 cap ([#54](https://github.com/LMPrado-DZ23/OmniRoute/pull/54))
