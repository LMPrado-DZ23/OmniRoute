- **fix(a11y):** the last icon on `/dashboard/cli-code` that was not hidden from the accessibility tree now is. Material Symbols renders by ligature, so an unhidden icon span is announced as the literal word it contains — this one read "warning" aloud before the sentence that already says what is wrong. axe's `button-name` rule passes either way (a control named `content_copy` *has* a name), so no tool reported it. The surface is now 153 of 153 hidden, and a test scans it and fails if a new one arrives unhidden. **Known wider gap, not touched here:** the same pattern appears 1630 times repository-wide and 1138 still lack `aria-hidden`, across 279 files — pre-existing, far beyond this release, and asserted nowhere because a repository-wide check would be a red that blocks a release rather than a guard.
