- **fix(docs):** all 41 translated READMEs now carry the fork-provenance notice that only the English one had. Without it, a reader arriving at `docs/i18n/<lang>/README.md` saw upstream's badges and upstream's install instructions with nothing saying which project they were reading: `npm install -g omniroute` installs the **upstream** package, not this fork, whose builds come from this repository's GitHub Releases, `ghcr.io/lmprado-dz23/omniroute`, or source. The notice is the same paragraph the English README carries, kept in English deliberately — it must not depend on a translation staying current, and it must say the same thing on every page — plus one line explaining that the upstream badges below it are credit rather than a download link for this fork. `check:doc-links` passes (172 docs, 1044 internal links) and the fabricated-claim gate finds nothing new.
