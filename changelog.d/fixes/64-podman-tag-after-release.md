- **fix(podman):** the Quadlet unit pinned `omniroute:next` while the README beside it said `:latest`, so the guidance disagreed with itself. Both now name the published stable. The original pin was correct when written — `:latest` did not resolve before a SemVer publish existed — and the comment recording that reason is rewritten rather than left behind as a justification that expired when v3.8.54 shipped ([#64](https://github.com/LMPrado-DZ23/OmniRoute/pull/64))
