- **fix(build):** the v3.8.55 installer build failed before it started: `check:native-deps` reported `@huggingface/transformers` missing, so the shared Next standalone job exited 1 and the Release was never created. The package itself has no install script — `onnxruntime-node`, which it depends on, does, and it is the only script-bearing package in that subtree. The runner's npm began refusing install scripts not listed in `allowScripts` between 2026-09-19 and 2026-09-20; it then drops the package **and exits 0**, which is why the guard exists at all. v3.8.54 built the same lockfile with the same allowlist the day before. `onnxruntime-node` joins the seven native-binding packages already allowed (`better-sqlite3`, `esbuild`, `@swc/core`, `@parcel/watcher`, `keytar`, `protobufjs`, `unrs-resolver`) — one package, the one the build actually needs. The other three npm flagged as unapproved (`@playwright/browser-chromium`, `bun`, `libxmljs2`, `opencode-ai`) stay unapproved, because nothing reported them missing.
