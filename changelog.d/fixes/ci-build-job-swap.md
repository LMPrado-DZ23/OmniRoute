- **fix(ci):** the `Build` job of `ci.yml` was cancelled on every full run of the audit branch — its `npm run build` step never finished. A single Turbopack build peaks at 14–16 GB RSS (the job's own comment), a hosted runner has 16 GB, and `--max-old-space-size` cannot bound Turbopack's native allocations (#6409). The job now provisions the same 10 GB of swap that `build.yml` and the Release-Green sweep already use (best effort: a runner that cannot provide swap builds as before; self-hosted runners skip it). Not yet proven — the next full run is the test.
