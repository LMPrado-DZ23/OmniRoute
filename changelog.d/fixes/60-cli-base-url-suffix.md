- **fix(cli-tools):** two CLI cards handed a `/v1` base URL to clients that append their own path, so the URL they produced was wrong. Goose's `OPENAI_HOST` is a host, not an OpenAI-compatible base — it appends `OPENAI_BASE_PATH` (default `v1/chat/completions`), so the card produced `…/v1/v1/chat/completions`; it now renders the origin and sets `OPENAI_BASE_PATH` explicitly. The 5dive card showed a `/v1` URL while 5dive points Claude seats at the Anthropic surface, which appends `/v1/messages`; the card now matches what `omniroute configure 5dive` already wrote ([#60](https://github.com/LMPrado-DZ23/OmniRoute/pull/60))
