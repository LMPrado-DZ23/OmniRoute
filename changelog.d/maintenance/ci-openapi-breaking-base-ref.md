- **maintenance(ci):** the OpenAPI breaking-change gate was wired to receive a bare `github.base_ref`, while the pr-test-policy step beside it passes `origin/{0}`. `check-openapi-breaking.mjs` resolves the base spec with `git show <BASE_REF>:docs/openapi.yaml`, and on a `pull_request` checkout (`refs/pull/N/merge`) there is no local branch by that name — only the remote-tracking ref — so the bare form would not resolve, the script would SKIP, and a skip exits 0 by design: green while measuring nothing, the fourth instance of that shape found in this release. **Latent, not live:** `ci.yml` currently runs only on `workflow_dispatch` in this fork, where `github.base_ref` is empty and the script falls back to its package.json-derived default, which is already origin-prefixed — verified in run 34097249734, which measured against `origin/release/v3.8.50`. Fixed so it is correct the day `ci.yml` runs on a pull request again, with a test that sorts BASE_REF consumers by what they actually do: anything reaching `git` must be origin-prefixed, while `check-pr-self-target.mjs` compares branch *names* and is asserted to still be name-only.
