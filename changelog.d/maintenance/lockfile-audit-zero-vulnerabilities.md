- **maintenance(deps):** `npm audit` reported 7 vulnerabilities (4 high) on the release tip; the lockfile now audits at **zero**, with production dependencies at zero both before and after. All seven were dev-only. Three were plain patch bumps (`js-yaml` 4.3.1→4.3.2 and 5.4.1→5.4.2, `csv-parse` 7.0.1→7.0.3, `joi` 18.2.3→18.2.9). The other four were one cluster: `@openai/codex-security` ≤0.1.30, an optional dev dependency of `promptfoo`, bundles vulnerable copies of `fflate` and `smol-toml` and pulls `extract-zip`, which has **no fixed release at any version** — the only way out is the parent no longer depending on it, which 0.1.31 does. `npm audit fix` would have taken that fix but rewrote 1255 lockfile lines including dozens of unrelated `@inquirer/*` packages, so the bump was made surgically instead. That bump also pinned `zod` to exactly 4.6.5, and because the project's range (`^4.5.4`) accepts it, npm promoted the single hoisted production `zod` from 4.5.4 to 4.6.5 purely to accommodate a dev tool — the root `zod` was pinned back to 4.5.4 and `codex-security` keeps its own nested copy. Net effect on what ships: two patch bumps (`js-yaml` 5.4.2, `yaml` 2.9.1). No package gained, lost or changed an install script (the same 15 as before), so the `allowScripts` behaviour that broke the v3.8.55 installer build is not reopened.
