- **refactor(combo):** `open-sse/services/combo.ts` 4080 → 3779 lines, with the auto-candidate builder extracted into its own module and the frozen file-size entry lowered to match — down only. Behaviour was held still and proven, not assumed: 700/700 selection cases identical against the pre-refactor modules (extracted read-only from the tip, seeded RNG, frozen clock, separate processes), equal `Math.random()` call counts per case, 120/120 byte-identical candidate rows for the builder split, and 598 previews with zero RNG calls inside `previewRoutingDecision` ([#53](https://github.com/LMPrado-DZ23/OmniRoute/pull/53))
