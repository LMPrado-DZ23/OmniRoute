- **fix(test):** the integration suite had been red on `release/v3.8.55` since before the tag, and the sweep said so every run. I read "the slow-suite job concluded `success`" as "the suite passed"; it does not — those jobs upload their report and succeed, and only the aggregator reads it. All six full sweeps on the release line failed with the same ~50 integration tests. Local reproduction of the 2026-09-25 sweep found five independent causes, none of them a regression in a shipped route: (1) 22 files failed the network guard because every chat dispatch warms the egress IP through `api64.ipify.org`/`api4.ipify.org` — `tests/_setup/isolateDataDir.ts` now pins the documented `OMNIROUTE_PROXY_ECHO_URL` override to a closed loopback port; (2) three files reached `aihorde.net` through `/api/v1/models` — they now inject an empty catalog the way `api-routes-critical` already did; `modelsDevSync.test.ts` is a genuine live test and is now gated behind `RUN_LIVE_TESTS=1`; `provider-journey.contract` moved its proxy host from `example.com` (a real domain) to a `.test` name; (3) the combo suites routed to `claude-3-5-sonnet-20241022`, which the model-lifecycle registry has rejected since it was retired on 2025-10-28 — 8 files now use `claude-sonnet-4-6`; (4) the three HTTP e2e suites boot `next dev` through `run-next-playwright.mjs`, which has no custom server to stamp the peer, so #69's "anonymous /v1 only from a provable local caller" answered them `AUTH_002` — they now send the same token-stamped header the custom server would (`tests/helpers/localPeerStamp.ts`); (5) `combo-failover-e2e` used an 80 ms per-target timeout, shorter than the request setup that runs before the first upstream fetch, so the timeout it claimed to test had not happened where it said. Not touched: the DRR fairness test in `quota-share.test.ts` and one call-log assertion in `combo-routing-e2e.test.ts`, still red and still being investigated.
- **fix(test):** `quota-share.test.ts` "DRR fairness" built its two steps without a `weight`, although its comment says "equal weight 100". An unset step weight resolves to 0 (`comboStructure`), and DRR treats a zero total weight as nothing to distribute and keeps definition order — so it saw `openai` six times out of six and reported DRR as bypassed. Both steps now carry `weight: 100`. Whether an unweighted quota-share combo should rotate evenly instead of following definition order is a product question I left alone.
- **fix(test):** what the first full `ci.yml` run on this branch (run 36211470629) still showed, and I had not been able to see from the sweep because it sets no `INITIAL_PASSWORD` and I ran without the network guard: the call-log id claim was still racy (saves of one combo's attempts run concurrently and each awaits between choosing an id and inserting, so "is it free in the table?" is a check-then-act — ids are now claimed synchronously, with a concurrent-save test); `memory-category-filter` sends an anonymous request that `ci.yml`'s `INITIAL_PASSWORD` turns into a 401 (now a management session); `chat-pipeline`'s "invalid API key" case got its 401 from a real `api.openai.com` and now serves that answer locally; the combo harness leaves a refusing fetch between tests instead of the real one (a combo can leave hedged/early-retry attempts running after its test ends; they then reached `api.anthropic.com`/`generativelanguage.googleapis.com` with a fake key and failed `ordered.test.ts` under the guard — a hypothesis from the log, to be confirmed by the next run); `resilience-http-e2e`'s exact settings-key list gained `credentialHealthCheck`; `request-logger-position-9154` looked the sort control up by a `title` that my own a11y fix (#7300230d2) replaced with a real `<label>`; the axe test gets a 30 s timeout (it hit the 5 s default on a hosted runner; my first attempt put it on the wrong test). `src/lib/db/core.ts` is rebaselined 1745 → 1770 with a note: the file already measured 1770 on `release/v3.8.55`, so `check:file-size` was red there. Still open, not touched: `batch-e2e-rate-limit` (`[BATCH] Throttle check` logs once, with no rate-limit headers, where the test expects two — nothing I could find forwards the upstream `x-ratelimit-*` headers to the batch processor) and the `Build` job (its `npm run build` step was cancelled, the intermittent hosted-runner OOM described in the sweep notes).
- **fix(test):** the third full CI run showed the quarantine fetch was not enough for `ordered.test.ts` — "asynchronous activity after the test ended: TypeError: fetch failed" (7 live attempts) — because `base.resetStorage()` puts the real fetch back and only then awaits, leaving a gap; the combo harness now ignores that assignment while it runs. `playground-studio.test.tsx` awaits `vi.dynamicImportSettled()` before teardown: the Build tab lazy-loads a hook, and when the import landed after the environment was gone it raised an `EnvironmentTeardownError` that failed the whole vitest job (2566/2566 tests passing) as an unhandled rejection.
- **fix(test):** the fifth full run (Build now green, Vitest green, integration 1/2 green) left three more: `api.spec.ts` expected the catalog's `type: invalid_api_key` on an anonymous `/api/v1/models`, but the client-API authz policy in front of it answers `AUTH_002` first when the peer cannot be proven local (this harness runs plain `next dev`); `combos-flow.spec.ts` expected `codex/gpt-5.5` for a manually typed `cx/gpt-5.5`, but #11433 deliberately keeps the typed prefix — the expectation has been stale since that change; and `batch-e2e-rate-limit` — whose batch processor calls this server's own `/v1` from inside it — now creates a real API key so those internal calls are not refused (the log showed one "no rate-limit headers" throttle check instead of two, consistent with the items never reaching the relay; not confirmed by a run).
- **fix(test):** with Build green the Playwright e2e shards ran for the first time on this branch and four more tests failed. One is mine: `api-keys-flow` accepted a native `confirm()` dialog to delete a key, and my own U5 change (d4d8918ad, 2026-09-11) moved 35 destructive confirmations to the shared ConfirmModal without touching the e2e suite, so the DELETE was never sent — it now confirms through the modal (`providers-management` already had the same fix for its delete step). Still red and not diagnosed, because the shards upload no Playwright report: `api-keys-flow` "Restrict mode provider scope" (the reopened permissions dialog never appears), `providers-management` (the import dialog has no visible "Close" button) and `radar-guided-setup`.
- **fix(ci):** the e2e shards now upload `test-results/` (screenshots, DOM snapshots) when a test fails, kept for a week, so the remaining Playwright failures can be diagnosed instead of guessed. **fix(test):** the sixth full run confirmed the `batch-e2e-rate-limit` hypothesis — with a real API key the batch items reach the relay and the processor logs `Throttle check - Request pressure: 0/100` and a 30 s throttle — and then showed the batch is owned by that key, so polling it anonymously answered 404; the poll and final read now send the key too.
- **fix(test):** the first Playwright artifacts (run 36223144748) settled `providers-management`: straight after saving a connection the page shows the connection list, with "Auto-fetch upstream models" switched off, and no model-import modal. The test waited 15 s for that modal's Close button. Auto-fetch became opt-in in #11805/#12098, so the step is now optional (dismiss the modal if it appears).
